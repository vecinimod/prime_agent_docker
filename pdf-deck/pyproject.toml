[project]
name = "pdf-deck"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["fpdf2>=2.8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pdf_deck"]
